#include "main.h"

/**
* _strlen - returns the length of a string
* @s: parameter passed
* Return: Always 0
*/
int _strlen(char *s)
{
	len = _strlen(*s);
	printf("strlen(*s): %d\n", len);
	return (0);

}
