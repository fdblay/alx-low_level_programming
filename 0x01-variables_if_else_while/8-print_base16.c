#include <stdio.h>

/**
 * main - prints all base 16 numerals in lowercase
 * Return: Always 0
 */

int main(void)
{
	int n;
	char lc;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}

	for (lc = 'a'; lc <= 'f'; la++)
	{
		putchar(la)
	}

	putchar(10);
	return (0);
}
