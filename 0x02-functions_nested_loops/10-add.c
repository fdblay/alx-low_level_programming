#include "main.h"
/**
* add - adds two integers and returns the result
* @a: first integer
* @b: second integer
* Return: result
*/
int add(int a, int b)
{
	result;

	result = (a + b);

	return (result);
}
